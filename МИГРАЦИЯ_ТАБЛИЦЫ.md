# 🔄 Чеклист миграции на новую Google таблицу

## 📝 Что нужно обновить

### 1. Frontend (Web-интерфейс)

#### ✅ `docs/config.js`

- **Строка 2**: Обновить URL развертывания (deployment URL)

```javascript
API_URL: "https://script.google.com/macros/s/НОВЫЙ_DEPLOYMENT_ID/exec";
```

---

### 2. Backend (Google Apps Script)

#### ✅ `backend/doGet.gs` (3 места)

**Старый ID**: `1C9t0s9sCwXlnAmeloKDo2La8FncDGBux_TNG3zF3QNs`  
**Новый ID**: `___________________________` ← Впиши сюда ID новой таблицы

- [ ] **Строка 65** - функция `getReferralData()`:

```javascript
const spreadsheetId = "НОВЫЙ_ID";
```

- [ ] **Строка 73** - функция `getPaymentsData()`:

```javascript
const spreadsheetId = "НОВЫЙ_ID";
```

- [ ] **Строка 84** - функция `getBonusTransactionsData()`:

```javascript
const spreadsheetId = "НОВЫЙ_ID";
```

---

#### ✅ `backend/ssr_renderer.gs` (1 место)

- [ ] **Строка 14** - функция `doGetOld()`:

```javascript
const spreadsheetId = "НОВЫЙ_ID";
```

---

### 3. Файлы, которые НЕ требуют изменений ✅

Следующие файлы используют `SpreadsheetApp.getActiveSpreadsheet()` и будут работать автоматически:

- ✅ `calculateMlmBonuses_trigger.gs`
- ✅ `calculateMonthlyBonus.gs`
- ✅ `updateReferalsTotals.gs`
- ✅ `masterTrigger.gs`
- ✅ `customMenu.gs`
- ✅ `autoIdAndDate.gs`
- ✅ `writeBonusTransaction.gs`
- ✅ Все остальные `.gs` файлы

---

## 🚀 Пошаговая инструкция по миграции

### Шаг 1: Подготовка новой таблицы

- [ ] Убедись, что все листы скопированы (`referals`, `payments`, `bonus_transactions`, `withdraw`)
- [ ] Проверь структуру столбцов в каждом листе
- [ ] Запиши ID новой таблицы (находится в URL): `https://docs.google.com/spreadsheets/d/НОВЫЙ_ID/edit`

### Шаг 2: Обновление кода

- [ ] Обновить `docs/config.js` (deployment URL)
- [ ] Обновить `backend/doGet.gs` (3 места)
- [ ] Обновить `backend/ssr_renderer.gs` (1 место)

### Шаг 3: Развертывание Google Apps Script

- [ ] Открыть Google Apps Script редактор в новой таблице
- [ ] Скопировать все `.gs` файлы из папки `backend/`
- [ ] Создать новое развертывание (Deploy → New deployment)
- [ ] Выбрать тип: Web app
- [ ] Execute as: Me
- [ ] Who has access: Anyone
- [ ] Скопировать новый Deployment URL

### Шаг 4: Настройка триггеров (ВАЖНО!)

Триггеры не копируются автоматически, нужно настроить вручную:

#### Обязательные триггеры:

1. **masterTrigger** (основной триггер)
   - Функция: `masterTrigger`
   - Тип события: From spreadsheet → On change
2. **Меню при открытии**
   - Функция: `onOpen`
   - Тип события: From spreadsheet → On open

#### Опциональные триггеры (если используются):

3. **onEdit** (если используется вместо masterTrigger)
   - Функция: `onEdit`
   - Тип события: From spreadsheet → On edit

### Шаг 5: Обновление frontend

- [ ] Обновить в `docs/config.js` новый Deployment URL
- [ ] Протестировать загрузку данных через веб-интерфейс

### Шаг 6: Тестирование

- [ ] Проверить загрузку рефералов через API
- [ ] Проверить загрузку платежей
- [ ] Проверить загрузку бонусных транзакций
- [ ] Добавить тестовый платеж и проверить расчет бонусов
- [ ] Проверить обновление агрегированных данных
- [ ] Проверить расчет месячных бонусов (через меню)

### Шаг 7: Очистка кеша

- [ ] В Apps Script редакторе запустить функцию `clearCache()` (если есть)
- [ ] Или подождать 2 минуты (время жизни кеша)

---

## 🔍 Как найти ID новой таблицы

ID таблицы находится в URL:

```
https://docs.google.com/spreadsheets/d/1C9t0s9sCwXlnAmeloKDo2La8FncDGBux_TNG3zF3QNs/edit
                                      ↑                                           ↑
                                      └────────────────ID таблицы─────────────────┘
```

---

## 🔍 Как найти Deployment URL

1. Открыть Apps Script редактор (Extensions → Apps Script)
2. Нажать Deploy → Manage deployments
3. Скопировать URL из активного развертывания
4. Формат: `https://script.google.com/macros/s/DEPLOYMENT_ID/exec`

---

## 🧪 Тестовые функции

После миграции запусти в Apps Script редакторе:

1. **Проверка структуры таблиц**:

```javascript
testTableStructure(); // из calculateMlmBonuses_trigger.gs
```

2. **Проверка агрегированных данных**:

```javascript
testTablesStructure(); // из updateReferalsTotals.gs
```

3. **Тестовый расчет для конкретного ID**:

```javascript
testReferalCalculation("227290741"); // из updateReferalsTotals.gs
```

---

## ⚠️ Возможные проблемы и решения

### Проблема 1: "Reference Error: getReferralData is not defined"

**Решение**: Убедись, что все файлы из папки `backend/` скопированы в Apps Script редактор

### Проблема 2: "Access denied / Permission denied"

**Решение**:

- Проверь настройки развертывания (Execute as: Me, Who has access: Anyone)
- Переразверни приложение заново

### Проблема 3: Триггеры не работают

**Решение**:

- Открыть Apps Script → Triggers
- Удалить старые триггеры (если есть)
- Создать новые по инструкции выше

### Проблема 4: Данные не обновляются

**Решение**:

- Проверь, что триггер `masterTrigger` настроен правильно
- Запусти вручную `updateReferalsTotals()` из редактора
- Проверь логи (View → Logs или View → Executions)

### Проблема 5: Frontend не загружает данные

**Решение**:

- Проверь Deployment URL в `docs/config.js`
- Открой консоль браузера (F12) и посмотри на ошибки
- Проверь, что развертывание активно (Manage deployments)

---

## 📞 Дополнительная информация

- Документация MLM системы: `backend/MLM_ИНСТРУКЦИЯ.md`
- Настройка триггеров: `backend/НАСТРОЙКА_ТРИГГЕРА.md`
- Тестирование: `backend/ТЕСТИРОВАНИЕ.md`
- Основная инструкция: `ИНСТРУКЦИЯ.md`

---

**Дата создания**: 05.01.2026  
**Версия**: 1.0
